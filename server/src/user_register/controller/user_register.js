const jwt = require("jsonwebtoken");
const User = require("../model/User");

module.exports =  {
    register: async(req,res) => {
        const {
            username,
            password,
            email
        } = req.body;

        
        

        
    },

    
}